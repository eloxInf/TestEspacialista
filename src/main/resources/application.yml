server:
  port: 9090

spring:
  security:
    user:
      name: angelo
      password: 123456
  datasource:
    url: jdbc:hsqldb:file:C:/localBD/EspInteg
    username: ${USER_BD}
    password: ${PASS_BD}
    driverClassName: org.hsqldb.jdbc.JDBCDriver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        SQL: DEBUG
security:
  keyEncript: ${KEY_ENCRIPT}
  timeToken: ${EXPIRATION_TOKEN}
validation:
  user:
    email: ${EXPRESION_EMAIL}

